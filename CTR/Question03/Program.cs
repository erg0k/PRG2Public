namespace Question03
{
    class Program
    {
        static void Main()
        {
            //(a1) Error, Customer is an abstract class and thus can't be instantiated
            //(a2) Error, mismatched class. cust2 should be defined with the Member class instead of Customer
        }
    }
}
